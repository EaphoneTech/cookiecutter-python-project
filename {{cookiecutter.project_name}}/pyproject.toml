[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "eaphone-g09-loader"
version = "1.6.0"
description = ".g09 file loader for python"
authors = ["Xiaoyu Guo <biggates2010@gmail.com>"]
readme = "README.md"
packages = [{ include = "eaphone" }]

{% if cookiecutter.inclue_sphinx -%}
[tool.poetry.group.docs]
optional = true

[tool.poetry.group.docs.dependencies]
myst-nb = "^1"
sphinx_rtd_theme = "^2"
sphinx-last-updated-by-git = "^0.3"
sphinx = "^7.2"
{% endif %}

{% if cookiecutter.include_pytest -%}
[tool.poetry.group.testing]
optional = true

[tool.poetry.group.testing.dependencies]
pytest = "^8.1"
pytest-cov = "^5"
{% endif %}

[[tool.poetry.source]]
name = "aliyun-mirror"
url = "https://mirrors.aliyun.com/pypi/simple/"
priority = "primary"

[[tool.poetry.source]]
name = "eaphone-protected"
url = "https://biggates-pypi.pkg.coding.net/python-projects/protected/simple/"
priority = "supplemental"

[tool.ruff.lint]
ignore = ["F821", "E501"]
select = [
    # pycodestyle
    "E",
    # Pyflakes
    "F",
    # pyupgrade
    "UP",
    # flake8-bugbear
    "B",
    # flake8-simplify
    "SIM",
    # isort
    "I",
]

[tool.ruff.lint.isort]
known-first-party = ["eaphone"]

[tool.pytest.ini_options]
addopts = "-ra --cov=【包名】 --cov-report=xml --cov-report=html:htmlcov --cov-report=term --junitxml=reports/test-result.xml"
